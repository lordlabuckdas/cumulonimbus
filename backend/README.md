# backend

**stack:** flask + mongodb

## endpoints

* `GET /api`
* `GET /api/os/<os>`
* `GET /api/domain/<domain>`
* `GET /api/workgroup/<workgroup>`

## setup

```shell
$ pip install -r requirements.txt
$ flask run
```
